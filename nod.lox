win=window(800,600)
win.init() 

while (!win.should_close()) {
    win.begin()
    win.clear()
    win.text(10, 10, "Hello, Lox!")
    win.end()
    if (win.key_down("KEY_SPACE")) {
        win.close()
    }
}
