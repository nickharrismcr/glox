{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Go",
      "type": "go",
      "request": "launch",
      "mode": "auto",       // or "exec" if you're running a binary
      "program": "${workspaceFolder}/main.go",
      "args": ["--debug","lox.lox"],  // <== your CLI args here
      "env": {},
      "cwd": "${workspaceFolder}"
    }
  ]
}